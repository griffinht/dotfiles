path="~/.local/state/calendar/calendar"
default_list="calendar"
default_command="list --startable"

# show more friendly due dates
humanize=True

# only show startable tasks (with no start date or past start date)
#startable=True
# no way to undo this :( ?
