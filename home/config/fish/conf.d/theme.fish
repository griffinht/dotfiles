# todo figure out the difference between interactive and non interactive?
# honestly i think this should be interactive only
# but there is also of course the issue of the application launcher not getting the right environment variables but whatever
# theme integration
source "$XDG_CACHE_HOME/theme/config.fish"
